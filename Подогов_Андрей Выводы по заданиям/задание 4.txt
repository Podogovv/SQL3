Оба запроса дают одинаковый вывод данных, но:
+ "group by" имеет выше производительность на больших данных;
+ "оконные функции" требуют дополнительных шагов при написании кода, в этом плане group by проще;
+ вывод одинаковый, но "оконные функции" требуют фильтрации (rn=1 одна строка на клиента)